# CivicPulse AI — Frontend Environment Variables
# ================================================
# Only browser-safe values here.
# NO server secrets, database credentials, or API keys.
#
# Vite exposes variables prefixed with VITE_ to the browser.

# Backend API base URL
VITE_API_BASE_URL=http://localhost:8000
